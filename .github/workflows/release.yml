name: Build & Release

on:
  push:
    branches:
      - main

jobs:
  build-and-release:
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install JS dependencies
        run: npm install --legacy-peer-deps

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.2'
          bundler-cache: true

      - name: Install CocoaPods
        run: cd ios && pod install

      - name: Import Distribution Certificate
        env:
          DISTRIBUTION_CERT_P12: ${{ secrets.DISTRIBUTION_CERT_P12 }}
          CERT_PASSWORD: ${{ secrets.CERT_PASSWORD }}
        run: |
          printf '%s' "$DISTRIBUTION_CERT_P12" | base64 -d > /tmp/cert.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import /tmp/cert.p12 -k build.keychain -P "$CERT_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain
          security set-keychain-settings -t 3600 -u build.keychain

      - name: Import Provisioning Profile
        env:
          PROVISIONING_PROFILE: ${{ secrets.PROVISIONING_PROFILE }}
        run: |
          printf '%s' "$PROVISIONING_PROFILE" | base64 -d > /tmp/profile.mobileprovision
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          cp /tmp/profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/

      - name: Write App Store Connect API Key
        env:
          APP_STORE_CONNECT_API_KEY: ${{ secrets.APP_STORE_CONNECT_API_KEY }}
        run: echo "$APP_STORE_CONNECT_API_KEY" > ios/fastlane/app_store_connect_api_key.p8

      - name: Write .env.staging
        env:
          ENV_STAGING: ${{ secrets.ENV_STAGING }}
        run: echo "$ENV_STAGING" > .env.staging

      - name: Write .env.production
        env:
          ENV_PRODUCTION: ${{ secrets.ENV_PRODUCTION }}
        run: echo "$ENV_PRODUCTION" > .env.production

      - name: Install Fastlane
        run: gem install fastlane

      - name: Verify Secrets & Files
        run: |
          echo "=== Checking certificates ==="
          security find-identity -v -p codesigning
          echo "=== Checking provisioning profiles ==="
          ls ~/Library/MobileDevice/Provisioning\ Profiles/
          echo "=== Checking API key ==="
          cat ios/fastlane/app_store_connect_api_key.json
          echo "=== Checking env files ==="
          ls -la .env.staging .env.production
          echo "=== Checking Xcode schemes ==="
          cd ios && xcodebuild -workspace carScrap.xcworkspace -list

      - name: Build & Upload Staging to TestFlight
        env:
          APP_STORE_CONNECT_API_KEY_PATH: ${{ github.workspace }}/ios/fastlane/app_store_connect_api_key.json
        run: cd ios && fastlane staging
        continue-on-error: true

      - name: Print build log on failure
        if: always()
        run: cat ~/Library/Logs/gym/carScrap-car\ scrap\ staging.log 2>/dev/null | tail -100 || echo "No log found"

      - name: Build & Upload Production to TestFlight
        env:
          APP_STORE_CONNECT_API_KEY_PATH: ${{ github.workspace }}/ios/fastlane/app_store_connect_api_key.json
        run: cd ios && fastlane production
        continue-on-error: true

      - name: Print production build log on failure
        if: always()
        run: cat ~/Library/Logs/gym/carScrap-carScrapProduction.log 2>/dev/null | tail -100 || echo "No log found"
