// Update this file with each OTA patch before pushing the -patch tag.
// Only shown in the Profile screen when an OTA bundle is active on the device.
const OTA_CHANGELOG = '';

export default OTA_CHANGELOG;
